<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= project.name %> Photos</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <style>
        body {
            background-color: black;
            color: #FFD700; /* Gold */
            font-family: 'Arial', sans-serif;
            text-align: center;
            padding-bottom: 50px;
        }
        .title {
            font-size: 2.5rem;
            font-weight: bold;
            margin-top: 2rem;
        }
        .photos-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 30px;
            padding: 20px;
            max-width: 1200px;
            margin: auto;
        }
        .photo-wrapper {
            width: 100%;
            max-width: 900px; /* Adjust max width */
            display: flex;
            justify-content: center;
        }
        .photo {
            width: 100%;
            max-width: 800px; /* Adjust max image size */
            height: auto;
            border-radius: 10px;
            transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
        }
        .photo:hover {
            transform: scale(1.05);
            box-shadow: 0px 4px 15px rgba(255, 215, 0, 0.6);
        }
        .back-button {
            display: inline-block;
            margin: 20px auto;
            padding: 10px 20px;
            background: linear-gradient(135deg, #FFD700, #FFA500);
            color: black;
            font-weight: 600;
            border-radius: 8px;
            transition: all 0.3s ease-in-out;
        }
        .back-button:hover {
            background: linear-gradient(135deg, #FFA500, #FFD700);
            transform: scale(1.05);
        }
    </style>
</head>
<body>

    <h1 class="title"><%= project.name %> - Photos</h1>
    <div class="photos-container">
        <% project.photos.forEach(photo => { %>
            <div class="photo-wrapper">
                <img src="<%= photo %>" class="photo">
            </div>
        <% }) %>
    </div>
    <script>
        // GSAP Animation
        gsap.from(".title", { opacity: 0, y: -50, duration: 1 });
        gsap.from(".photo-wrapper", { opacity: 0, stagger: 0.3, scale: 0.9, duration: 0.8 });
        gsap.from(".back-button", { opacity: 0, y: 20, duration: 0.8, delay: 0.5 });
    </script>

</body>
</html>
