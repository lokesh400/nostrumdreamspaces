<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Upload Photo</title>
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&amp;display=swap" rel="stylesheet"/>

    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet"/>

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
        .button {
            display: inline-block;
            padding: 12px 20px;
            background: linear-gradient(135deg, #FFD700, #FFA500);
            color: black;
            font-weight: 600;
            border-radius: 8px;
            transition: all 0.3s ease-in-out;
            box-shadow: 0 4px 10px rgba(255, 215, 0, 0.5);
        }
        .button:hover {
            background: linear-gradient(135deg, #FFA500, #FFD700);
            transform: scale(1.05);
        }
        .form-container {
            max-width: 500px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 12px;
            padding: 30px;
            border: 1px solid rgba(255, 215, 0, 0.2);
        }
    </style>
</head>

<body class="bg-black flex items-center justify-center min-h-screen text-yellow-400">
    <div class="form-container text-center">
        <h2 class="text-3xl font-bold mb-6">Upload Photo</h2>

        <form action="/upload-photo/<%= project._id %>" method="POST" enctype="multipart/form-data">
            <label class="block text-lg font-medium mb-2">Select an image:</label>
            <input type="file" name="photo" class="w-full px-4 py-2 mb-4 bg-gray-800 text-white rounded-lg">

            <button type="submit" class="button w-full">
                <i class="fas fa-upload"></i> Upload Photo
            </button>
        </form>

        <a href="/" class="mt-4 inline-block text-yellow-300 hover:text-yellow-500">
            <i class="fas fa-arrow-left"></i> Back to Home
        </a>
    </div>
</body>
</html>
